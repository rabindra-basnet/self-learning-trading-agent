"""HTTP layer aggregation + composition root.

- app/api/main.py: FastAPI app, middleware, lifespan wiring.
- app/api/routers.py: router aggregation (import-layer).
- app/api/di.py: container composition (composition root layer).
"""
